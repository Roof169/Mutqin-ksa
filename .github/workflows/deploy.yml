name: 🚀 Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3
      
      - name: 📋 Setup Pages
        uses: actions/configure-pages@v3
      
      - name: 📦 Upload artifact
        uses: actions/upload-pages-artifact@v1
        with:
          path: '.'
      
      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
      
      - name: ✅ Deployment Success
        run: |
          echo "✅ تم النشر بنجاح!"
          echo "الموقع: https://roof169.github.io/Mutqin-ksa"
